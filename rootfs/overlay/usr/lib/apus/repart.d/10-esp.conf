# Installer layout, partition 1: EFI system partition, mounted at /boot.
# Starts as a copy of the live ESP (systemd-boot, kernel, initramfs).
[Partition]
Type=esp
Format=vfat
SizeMinBytes=512M
SizeMaxBytes=512M
CopyFiles=/run/apus-install/esp:/
