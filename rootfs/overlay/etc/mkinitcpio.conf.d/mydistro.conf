# Generic initramfs. No `autodetect`: the image is built in a container and
# must boot on whatever machine it is installed to.
MODULES=(virtio_pci virtio_blk virtio_scsi overlay)
HOOKS=(base systemd modconf keyboard block filesystems fsck sd-volatile)
