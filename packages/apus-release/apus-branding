#!/bin/sh
# Replace the os-release that `filesystem` ships with Apus's.
# /etc/os-release is a symlink to it; /etc/issue reads PRETTY_NAME from it.
install -m644 /usr/share/apus/os-release /usr/lib/os-release
