# The test client of the compositor, as an app. Its program is a system
# program, so `exec` is an absolute path and not a path in the bundle.
id = org.mydistro.hello
name = Hello
exec = /usr/bin/mydistro-hello-client
color = 3070F0
